import { SubmitButton } from "./form-buttons.styles";

const FormSubmitButton = ({ buttonText }) => {
  return <SubmitButton type="submit">{buttonText}</SubmitButton>;
};

export default FormSubmitButton;
