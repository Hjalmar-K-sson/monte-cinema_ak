import { HomepageLinkButton } from "./form-buttons.styles";

const HomePageButton = ({ href, buttonText }) => {
  return <HomepageLinkButton>{buttonText}</HomepageLinkButton>;
};

export default HomePageButton;
